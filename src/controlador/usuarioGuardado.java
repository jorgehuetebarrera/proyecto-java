
package controlador;
import modelo.Usuario;
import vista.Login;


public class usuarioGuardado extends Login {
  if (!recuerdame.isSelected()){usuario.setText("");
       } else {
          String usuarioGuardado =  usuario.getText();
           usuario.setText("porno");
        }
    }                              
}
